import './admin.css';

export const metadata = { title: 'KDSL Admin', robots: 'noindex, nofollow' };

export default function RootAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
